package com.SystemDesign.LowLevelDesign.RateLimiter;

public class RateLimitFactory {
/*    public static RateLimiter createRateLimiter(RateLimitType type, RateLimitConfig config) {
return switch(type){
    case  TOKEN_BUCKET ->new TokenBucketRateLimiter(config);
    case FIXED_WINDOW->new FixedWindowRateLimiter(config);
    case SLIDING_WINDOW->new SlidingWindowRateLimiter(config);
    case LEAKY_BUCKET->new LeakyBucketRateLimiter(config);
   default-> throw new IllegalArgumentException("UnknownError");
};
}*/

}